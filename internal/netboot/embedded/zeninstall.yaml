name: Zen Installer Arch
enabled: true
menu_group: linux
local_base: netboot/zeninstall
versions:
    - codename: current
      name: current
      arch: amd64
      enabled: true
      install_type: legacy
      cmdline: "archisobasedir=arch net.ifnames=0"
