name: VyOS
enabled: true
menu_group: linux
local_base: netboot/vyos
versions:
    - codename: rolling
      name: 1.5 rolling
      arch: amd64
      enabled: true
      install_type: legacy
      cmdline: "boot=live components hostname=vyos username=live nopersistence noautologin union=overlay console=tty0 net.ifnames=0 biosdevname=0"
